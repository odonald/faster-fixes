---
title: Setup
description: Connect the MCP server to your AI coding agent to fetch and resolve client feedback directly from your terminal.
---

Install the MCP server with a single command (Claude Code):

```bash
claude mcp add faster-fixes -s project \
  --env FASTER_FIXES_TOKEN=ff_agent_xxx \
  --env FASTER_FIXES_PROJECT=proj_xxx \
  -- npx -y @fasterfixes/mcp
```

For other editors, add the JSON config below.

## Environment variables

| Variable | Required | Description |
|----------|----------|-------------|
| `FASTER_FIXES_TOKEN` | Yes | Agent token from Organization Settings → Agent Tokens (prefix: `ff_agent_`) |
| `FASTER_FIXES_PROJECT` | Yes | Project ID from Project Settings (prefix: `proj_`) |
| `FASTER_FIXES_URL` | No | API base URL (defaults to `https://www.faster-fixes.com`) |

## Claude Code

Create `.mcp.json` in your project root:

```json
{
  "mcpServers": {
    "faster-fixes": {
      "command": "npx",
      "args": ["-y", "@fasterfixes/mcp"],
      "env": {
        "FASTER_FIXES_TOKEN": "ff_agent_xxx",
        "FASTER_FIXES_PROJECT": "proj_xxx"
      }
    }
  }
}
```

## Self-hosted instances

Add `FASTER_FIXES_URL` pointing at your instance to every config below, and keep the token out of the repo by referencing an environment variable:

```json
"env": {
  "FASTER_FIXES_URL": "https://fixes.example.com",
  "FASTER_FIXES_PROJECT": "proj_xxx",
  "FASTER_FIXES_TOKEN": "${FASTER_FIXES_TOKEN}"
}
```

One agent token covers every project of the organisation; a new project only needs its own `FASTER_FIXES_PROJECT`. The **Integrations** page of your dashboard shows a ready-to-copy version of this with your URL filled in.

## Cursor

Create `.cursor/mcp.json` in your project root:

```json
{
  "mcpServers": {
    "faster-fixes": {
      "command": "npx",
      "args": ["-y", "@fasterfixes/mcp"],
      "env": {
        "FASTER_FIXES_TOKEN": "ff_agent_xxx",
        "FASTER_FIXES_PROJECT": "proj_xxx"
      }
    }
  }
}
```

## VS Code (GitHub Copilot)

Create `.vscode/mcp.json` in your project root:

```json
{
  "servers": {
    "faster-fixes": {
      "command": "npx",
      "args": ["-y", "@fasterfixes/mcp"],
      "env": {
        "FASTER_FIXES_TOKEN": "ff_agent_xxx",
        "FASTER_FIXES_PROJECT": "proj_xxx"
      }
    }
  }
}
```

Note: VS Code uses `servers` instead of `mcpServers`.

## Windsurf

Add to `~/.codeium/windsurf/mcp_config.json`:

```json
{
  "mcpServers": {
    "faster-fixes": {
      "command": "npx",
      "args": ["-y", "@fasterfixes/mcp"],
      "env": {
        "FASTER_FIXES_TOKEN": "ff_agent_xxx",
        "FASTER_FIXES_PROJECT": "proj_xxx"
      }
    }
  }
}
```

## Codex

Add to `.codex/config.toml` in your project root:

```toml
[mcp_servers.faster-fixes]
command = "npx"
args = ["-y", "@fasterfixes/mcp"]

[mcp_servers.faster-fixes.env]
FASTER_FIXES_TOKEN = "ff_agent_xxx"
FASTER_FIXES_PROJECT = "proj_xxx"
```

## Zed

Add to `~/.config/zed/settings.json`:

```json
{
  "context_servers": {
    "faster-fixes": {
      "command": {
        "path": "npx",
        "args": ["-y", "@fasterfixes/mcp"],
        "env": {
          "FASTER_FIXES_TOKEN": "ff_agent_xxx",
          "FASTER_FIXES_PROJECT": "proj_xxx"
        }
      }
    }
  }
}
```

